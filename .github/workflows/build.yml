name: Deploy em Servidor Front (Clean Build All)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    name: Build e Deploy da Aplicação (Do Zero)
    runs-on: self-hosted

    steps:
      - name: (FRONT) - 📥 Baixar código do repositório
        uses: actions/checkout@v4

      #FRONTEND ----------------------------------------------------------------------------
      - name: dkjd
        run: ls
      
      - name: (FRONT) - 🛑 Desligar e remover serviços antigos
        run: docker compose -f docker-compose.dev.yml down --rmi all --volumes || true

      - name: (FRONT) - 🚀 Reconstruir e rodar com Docker Compose (sem cache)
        run: docker compose -f docker-compose.dev.yml up --build -d


      # REMOVIDO: - 🧹 Limpeza completa do sistema Docker (era agressivo)
      # - name: (FRONT) - 🧹 Limpeza completa do sistema Docker
      #   if: always()
      #   run: docker system prune -af



